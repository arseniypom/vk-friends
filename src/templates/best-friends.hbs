<div class="friends">
    {{#each items}}
    <div class="friend">
        <div class="friend-info">
            <img src="{{photo_100}}"/>
            <div class="friend-name">{{first_name}} {{last_name}}</div>
        </div>
        <button class="friend-button" data-role="remove-from-best-friends" data-userid="{{id}}">Переместить</button>
    </div>
    {{/each}}
</div>